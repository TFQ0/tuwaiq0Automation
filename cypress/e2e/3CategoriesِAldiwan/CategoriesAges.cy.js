/// <reference types="cypress" />

describe('Categories - Ages Tests - Aldiwan', () => {
  
  beforeEach(() => {
    cy.visit('https://www.aldiwan.net/')
  })

  it('should navigate to Ages category', () => {
    // Use Cypress Studio to record:
    // Navigate to the Ages category page
  })

  it('should filter by specific age period', () => {
    // Record filtering by age/era
  })

  it('should display poets from selected age', () => {
    // Record viewing poets from a specific age
  })

})

