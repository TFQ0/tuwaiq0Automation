/// <reference types="cypress" />

describe('Categories - Countries Tests - Aldiwan', () => {
  
  beforeEach(() => {
    cy.visit('https://www.aldiwan.net/')
  })

  it('should navigate to Countries category', () => {
    // Use Cypress Studio to record:
    // Navigate to the Countries category page
  })

  it('should filter by specific country', () => {
    // Record filtering by country
  })

  it('should display poets from selected country', () => {
    // Record viewing poets from a specific country
  })

})

