{
  "name": "falakauto123",
  "version": "1.0.0",
  "description": "Falak Automation Test Suite",
  "scripts": {
    "test": "cypress run --spec 'cypress/e2e/TestSuiteIntegrationFalak.cy.js'",
    "test:headed": "cypress open --e2e --spec 'cypress/e2e/TestSuiteIntegrationFalak.cy.js'",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "clean:reports": "node -e \"const fs=require('fs'); const path=require('path'); ['cypress/videos','cypress/screenshots','cypress/reports'].forEach(dir=>{if(fs.existsSync(dir)){fs.rmSync(dir,{recursive:true,force:true}); fs.mkdirSync(dir,{recursive:true}); console.log('Cleaned: '+dir);}});\""
  },
  "devDependencies": {
    "cypress": "^15.3.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}

